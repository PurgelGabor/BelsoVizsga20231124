function osszesOszto(szam) {
    var i;
    if (szam % i == 0) {
        var osztok = [];
        osztok.push(i);
    }
}
function parosDarab(szam) {
    for (var i = 0; szam.length; i++) {
        if (szam[i] % 2 == 0) {
            return szam[i];
        }
    }
}
function fuggvenyhivasPalindrom() {
}
